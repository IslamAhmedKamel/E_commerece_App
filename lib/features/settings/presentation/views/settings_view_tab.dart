import 'package:flutter/material.dart';

class SettingsViewTab extends StatelessWidget {
  const SettingsViewTab({super.key});

  @override
  Widget build(BuildContext context) {
    return SafeArea(child: Center(child: Text("Settings View")));
  }
}
