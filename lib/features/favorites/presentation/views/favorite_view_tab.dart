import 'package:flutter/material.dart';

class FavoriteViewTab extends StatelessWidget {
  const FavoriteViewTab({super.key});

  @override
  Widget build(BuildContext context) {
    return SafeArea(child: Center(child: Text("Favorites View")));
  }
}
